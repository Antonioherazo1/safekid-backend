services:
  api:
    build: .
    container_name: safekid-api
    restart: unless-stopped
    ports:
      - "127.0.0.1:8001:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - API_KEY_SALT=${API_KEY_SALT}
    env_file:
      - .env
